services:
  - type: web
    name: smart-tech-hub
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 18.18.0
      - key: JWT_SECRET
        generateValue: true
      - key: ADMIN_PASSWORD
        value: admin
